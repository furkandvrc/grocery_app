import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:grocery_app/screen/auth/preregistered_screen/controller/preregistered_screen_controller.dart';

class PreregisteredView extends GetView<PreregisteredController> {
  const PreregisteredView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
